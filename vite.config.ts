// vite.config.ts
import { defineConfig } from 'vite';

export default defineConfig({
    // This is the only line you need to add or edit.
    // It tells Vite how to create the correct paths for GitHub Pages.
    base: '/alchemist-forge/',

    // The plugins array can be empty.
    plugins: [],
});