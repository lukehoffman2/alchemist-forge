// jest.config.js
module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'jest-environment-jsdom',
    // Optional: setupFilesAfterEnv can be used for global setup
    // setupFilesAfterEnv: ['<rootDir>/src/setupTests.ts'],
};